"use client";
import { FormEvent,useEffect,useState } from "react";
import { supabase } from "../lib/supabase";
import PersistenceBridge from "./persistence-bridge";
type AuthMode="signin"|"signup";type AuthClient=NonNullable<typeof supabase>;
function Mark({large=false}:{large?:boolean}){return <img className={large?"auth-logo large":"auth-logo"} src="/mah-buddy-logo.svg" alt="Mah Buddy"/>}
function GoogleMark(){return <svg className="google-svg" viewBox="0 0 48 48" aria-hidden="true"><path fill="#4285F4" d="M24 9.5c3.5 0 6.6 1.2 9 3.5l6.7-6.7C35.6 2.4 30.2 0 24 0 14.6 0 6.5 5.4 2.6 13.3l7.8 6.1C12.3 13.7 17.6 9.5 24 9.5Z"/><path fill="#34A853" d="M2.6 13.3A24 24 0 0 0 0 24c0 4 1 7.8 2.8 11.1l7.8-6.1A14.5 14.5 0 0 1 9.5 24c0-2.2.5-4.3.9-4.6l-7.8-6.1Z"/><path fill="#FBBC05" d="M2.8 35.1C6.8 42.8 14.8 48 24 48c6 0 11.5-2 15.4-5.5l-7.5-5.8c-2.1 1.4-4.8 2.3-7.9 2.3-6.3 0-11.7-4.2-13.6-9.9l-7.6 6Z"/><path fill="#EA4335" d="M39.4 42.5c4.2-3.8 6.6-9.5 6.6-16.5 0-1.6-.2-3-.5-4.5H24v9h12.2c-.6 3-2.3 5.3-4.3 6.9l7.5 5.1Z"/></svg>}
function Intro({onStart}:{onStart:()=>void}){return <main className="auth-intro mb-onboarding"><div className="intro-top"><span>MAH BUDDY</span><span>YOUR AI STUDY COMPANION</span></div><section className="intro-hero"><div className="intro-orbit"><span className="orbit-dot one"/><span className="orbit-dot two"/><div className="intro-logo-wrap"><Mark large/></div></div><div className="intro-copy"><p className="intro-kicker">LEARN · PRACTISE · GROW</p><h1>Meet your<br/><em>Mah Buddy.</em></h1><p>Your calm AI study companion for conversations, explanations, flashcards, quizzes and voice learning.</p><button className="intro-start" onClick={onStart}>Get Started <span>→</span></button></div></section><div className="intro-bottom"><span>01</span><i/><span>READY WHEN YOU ARE</span></div></main>}
export default function AuthGate({children}:{children:React.ReactNode}){const[user,setUser]=useState<any>(null),[mode,setMode]=useState<AuthMode>("signin"),[started,setStarted]=useState(false),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[resetSent,setResetSent]=useState(false);useEffect(()=>{if(!supabase)return;const auth:AuthClient=supabase;auth.auth.getUser().then(({data})=>setUser(data.user));const{data:listener}=auth.auth.onAuthStateChange((_e,s)=>setUser(s?.user??null));return()=>listener.subscription.unsubscribe()},[]);if(!supabase)return <main className="auth-fallback"><Mark/><h1>Mah Buddy</h1><p>Add the Supabase environment variables in Vercel to enable accounts.</p></main>;const auth:AuthClient=supabase;if(user)return <><PersistenceBridge userId={user.id}/>{children}</>;async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMessage("");setResetSent(false);const r=mode==="signin"?await auth.auth.signInWithPassword({email,password}):await auth.auth.signUp({email,password,options:{emailRedirectTo:window.location.origin}});setBusy(false);if(r.error)setMessage(r.error.message);else if(mode==="signup")setMessage("Account created. Check your email if confirmation is required.")}async function google(){setMessage("");const{error}=await auth.auth.signInWithOAuth({provider:"google",options:{redirectTo:window.location.origin}});if(error)setMessage(error.message)}async function reset(){if(!email.trim()){setMessage("Enter your email address first.");return}setBusy(true);setMessage("");const{error}=await auth.auth.resetPasswordForEmail(email.trim(),{redirectTo:window.location.origin});setBusy(false);if(error)setMessage(error.message);else setResetSent(true)}if(!started)return <Intro onStart={()=>setStarted(true)}/>;return <main className="auth-screen mb-auth"><section className="auth-side"><div className="auth-side-brand"><Mark/><span>Mah Buddy</span></div><p className="auth-eyebrow">YOUR AI STUDY COMPANION</p><h1>Learn with a<br/><em>buddy.</em></h1><div className="auth-rule"/><p className="auth-copy">Ask questions, practise with quizzes and flashcards, or switch to voice mode when you want to talk it through.</p><div className="auth-preview"><div>CHAT</div><div>QUIZ</div><div>VOICE</div></div></section><section className="auth-form-card"><Mark/><div className="auth-form-heading"><span>WELCOME BACK</span><h2>{mode==="signin"?"Sign in":"Create your account"}</h2><p>{mode==="signin"?"Pick up where you left off.":"Start building your personal study space."}</p></div><div className="auth-tabs"><button className={mode==="signin"?"active":""} onClick={()=>setMode("signin")}>Sign in</button><button className={mode==="signup"?"active":""} onClick={()=>setMode("signup")}>Sign up</button></div><button className="google-btn" onClick={google}><GoogleMark/> <span>Continue with Google</span></button><div className="auth-or"><span/>or continue with email<span/></div><form onSubmit={submit}><label>Email address<input type="email" required placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required minLength={6} placeholder="At least 6 characters" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="auth-submit" disabled={busy}>{busy?"Please wait…":mode==="signin"?"Sign in":"Create account"}</button></form>{mode==="signin"&&!resetSent&&<button className="auth-link" onClick={reset}>Forgot password?</button>}{resetSent&&<p className="auth-msg">Password reset email sent. Check your inbox.</p>}{message&&!resetSent&&<p className="auth-msg">{message}</p>}<button className="auth-switch" onClick={()=>{setMode(mode==="signin"?"signup":"signin");setMessage("")}}>{mode==="signin"?"New to Mah Buddy? Sign up":"Already have an account? Sign in"}</button></section></main>}

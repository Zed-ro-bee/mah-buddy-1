"use client";
import { FormEvent,useEffect,useState } from "react";
import { supabase } from "../lib/supabase";
import PersistenceBridge from "./persistence-bridge";
type AuthMode="signin"|"signup";type AuthClient=NonNullable<typeof supabase>;
function Mark(){return <div className="auth-mark">M</div>}
function Intro({onStart}:{onStart:()=>void}){return <main className="auth-intro"><div className="intro-title"><h1>Mah Buddy</h1><div className="intro-rule"/></div><div className="intro-hero"><div className="intro-art"><div className="intro-logo"><Mark/><span>Mah Buddy</span></div></div><div className="intro-foot"><i>Your AI study companion</i><span>UI / UX</span></div></div><button className="intro-start" onClick={onStart}>Get Started <span>→</span></button></main>}
export default function AuthGate({children}:{children:React.ReactNode}){const[user,setUser]=useState<any>(null),[mode,setMode]=useState<AuthMode>("signin"),[started,setStarted]=useState(false),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[resetSent,setResetSent]=useState(false);useEffect(()=>{if(!supabase)return;const auth:AuthClient=supabase;auth.auth.getUser().then(({data})=>setUser(data.user));const{data:listener}=auth.auth.onAuthStateChange((_e,s)=>setUser(s?.user??null));return()=>listener.subscription.unsubscribe()},[]);if(!supabase)return <main className="auth-fallback"><Mark/><h1>Mah Buddy</h1><p>Add the Supabase environment variables in Vercel to enable accounts.</p></main>;const auth:AuthClient=supabase;if(user)return <><PersistenceBridge userId={user.id}/>{children}</>;async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMessage("");setResetSent(false);const r=mode==="signin"?await auth.auth.signInWithPassword({email,password}):await auth.auth.signUp({email,password,options:{emailRedirectTo:window.location.origin}});setBusy(false);if(r.error)setMessage(r.error.message);else if(mode==="signup")setMessage("Account created. Check your email if confirmation is required.")}async function google(){setMessage("");const{error}=await auth.auth.signInWithOAuth({provider:"google",options:{redirectTo:window.location.origin}});if(error)setMessage(error.message)}async function reset(){if(!email.trim()){setMessage("Enter your email address first.");return}setBusy(true);setMessage("");const{error}=await auth.auth.resetPasswordForEmail(email.trim(),{redirectTo:window.location.origin});setBusy(false);if(error)setMessage(error.message);else setResetSent(true)}if(!started)return <Intro onStart={()=>setStarted(true)}/>;return <main className="auth-screen"><section className="auth-side"><p>MAH BUDDY</p><h1>Sign In /<br/>Create Account</h1><div className="auth-rule"/><p className="auth-copy">Explore the user-friendly login interface for Mah Buddy.</p><div className="auth-art"><div className="auth-phone blue"><span>Login</span><i/><i/><b>Sign in</b></div><div className="auth-phone pink"><span>Mah Buddy</span><i/><i/><b>Get started</b></div></div></section><section className="auth-form-card"><Mark/><div className="auth-tabs"><button className={mode==="signin"?"active":""} onClick={()=>setMode("signin")}>Sign in</button><button className={mode==="signup"?"active":""} onClick={()=>setMode("signup")}>Sign up</button></div><button className="google-btn" onClick={google}>G&nbsp;&nbsp; Continue with Google</button><div className="auth-or">or continue with email</div><form onSubmit={submit}><input type="email" required placeholder="Email address" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" required minLength={6} placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="auth-submit" disabled={busy}>{busy?"Please wait…":mode==="signin"?"Sign in":"Create account"}</button></form>{mode==="signin"&&!resetSent&&<button className="auth-link" onClick={reset}>Forgot password?</button>}{resetSent&&<p className="auth-msg">Password reset email sent. Check your inbox.</p>}{message&&!resetSent&&<p className="auth-msg">{message}</p>}<button className="auth-link" onClick={()=>{setMode(mode==="signin"?"signup":"signin");setMessage("")}}>{mode==="signin"?"New to Mah Buddy? Sign up":"Already have an account? Sign in"}</button></section></main>}

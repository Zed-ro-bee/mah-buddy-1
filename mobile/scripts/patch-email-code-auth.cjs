const fs = require('fs');
const path = require('path');

const file = path.join(__dirname, '..', 'App.tsx');
let source = fs.readFileSync(file, 'utf8');

if (!source.includes("const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [authBusy, setAuthBusy] = useState(false);")) {
  throw new Error('Expected native auth state was not found in App.tsx');
}
source = source.replace(
  "const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [authBusy, setAuthBusy] = useState(false);",
  "const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [authBusy, setAuthBusy] = useState(false); const [verificationPending, setVerificationPending] = useState(false); const [verificationCode, setVerificationCode] = useState('');"
);

const oldSignUp = "async function signUp() { if (!supabase) return Alert.alert('Setup needed', 'The native app needs the Supabase URL and publishable key in its environment configuration.'); if (!email.trim() || !password) return Alert.alert('Missing details', 'Enter your email and password.'); if (password.length < 6) return Alert.alert('Password too short', 'Use at least 6 characters.'); setAuthBusy(true); const { data, error } = await supabase.auth.signUp({ email: email.trim(), password }); setAuthBusy(false); if (error) return Alert.alert('Sign up failed', error.message); if (!data.session) Alert.alert('Check your email', 'Your account was created. Complete email verification if required, then sign in.'); }";
const newSignUp = "async function signUp() { if (!supabase) return Alert.alert('Setup needed', 'The native app needs the Supabase URL and publishable key in its environment configuration.'); if (!email.trim() || !password) return Alert.alert('Missing details', 'Enter your email and password.'); if (password.length < 6) return Alert.alert('Password too short', 'Use at least 6 characters.'); setAuthBusy(true); const { data, error } = await supabase.auth.signUp({ email: email.trim(), password }); setAuthBusy(false); if (error) return Alert.alert('Sign up failed', error.message); if (data.session) { setVerificationPending(false); return; } setVerificationCode(''); setVerificationPending(true); Alert.alert('Verification code sent', `We sent a verification code to ${email.trim()}. Enter it in Mah Buddy to finish creating your account.`); }\n  async function verifyEmailCode() { if (!supabase) return; const code = verificationCode.replace(/\\D/g, '').slice(0, 6); if (!email.trim() || code.length !== 6) return Alert.alert('Enter the code', 'Enter the 6-digit verification code from your email.'); setAuthBusy(true); const { error } = await supabase.auth.verifyOtp({ email: email.trim(), token: code, type: 'email' }); setAuthBusy(false); if (error) return Alert.alert('Verification failed', error.message); setVerificationPending(false); setVerificationCode(''); Alert.alert('Email verified', 'Your account is ready.'); }\n  async function resendVerificationCode() { if (!supabase || !email.trim()) return; setAuthBusy(true); const { error } = await supabase.auth.resend({ type: 'signup', email: email.trim() }); setAuthBusy(false); if (error) return Alert.alert('Could not resend', error.message); Alert.alert('Code sent', `A new verification code was sent to ${email.trim()}.`); }";
if (!source.includes(oldSignUp)) throw new Error('Expected signUp function was not found in App.tsx');
source = source.replace(oldSignUp, newSignUp);

const authRegex = /  if \(!session\) return <SafeAreaView[\s\S]*?\n\n  if \(profileOpen\)/;
const newAuth = `  if (!session) return <SafeAreaView style={styles.safe}><KeyboardAvoidingView style={styles.authContainer} behavior={Platform.OS === 'ios' ? 'padding' : undefined}><View style={styles.authCard}><Text style={styles.title}>Mah Buddy</Text><Text style={styles.subtitle}>{verificationPending ? 'Enter the verification code sent to your email' : 'Your AI study buddy'}</Text>{verificationPending ? <><TextInput value={verificationCode} onChangeText={v => setVerificationCode(v.replace(/\D/g, '').slice(0, 6))} placeholder="6-digit code" placeholderTextColor="#8a8f98" keyboardType="number-pad" maxLength={6} style={styles.authInput} /><Pressable disabled={authBusy} onPress={verifyEmailCode} style={styles.primary}><Text style={styles.primaryText}>{authBusy ? 'Please wait…' : 'Verify email'}</Text></Pressable><Pressable disabled={authBusy} onPress={resendVerificationCode} style={styles.secondary}><Text style={styles.secondaryText}>Resend code</Text></Pressable><Pressable disabled={authBusy} onPress={() => { setVerificationPending(false); setVerificationCode(''); }}><Text style={styles.action}>Back</Text></Pressable></> : <><TextInput autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail} placeholder="Email" placeholderTextColor="#8a8f98" style={styles.authInput} /><TextInput secureTextEntry value={password} onChangeText={setPassword} placeholder="Password" placeholderTextColor="#8a8f98" style={styles.authInput} /><Pressable disabled={authBusy} onPress={signIn} style={styles.primary}><Text style={styles.primaryText}>{authBusy ? 'Please wait…' : 'Sign in'}</Text></Pressable><Pressable disabled={authBusy} onPress={signUp} style={styles.secondary}><Text style={styles.secondaryText}>Create account</Text></Pressable>{!supabase && <Text style={styles.setup}>Native authentication is awaiting Supabase environment configuration.</Text>}</>}</View></KeyboardAvoidingView></SafeAreaView>;

  if (profileOpen)`;
if (!authRegex.test(source)) throw new Error('Expected native auth screen was not found in App.tsx');
source = source.replace(authRegex, newAuth);

fs.writeFileSync(file, source);
console.log('Mah Buddy native email-code auth patch applied.');

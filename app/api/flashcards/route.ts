import { google } from "@ai-sdk/google";
import { generateText } from "ai";
import { NextResponse } from "next/server";
import { createClient } from "@supabase/supabase-js";
const LEVELS:Record<string,string>={Foundation:"A1–A2: use very simple English, short sentences, basic concepts, and clear everyday examples. Define unfamiliar terms immediately.",Developing:"B1: use clear everyday English, moderate concepts, practical examples, and moderate explanation depth.",Proficient:"B2: use fluent natural English, subject terminology with brief clarification, deeper explanations, reasoning, and useful examples.",Advanced:"C1: use precise academic English, nuanced vocabulary, detailed reasoning, and fewer simplifications.",Expert:"C2: use sophisticated precise English, advanced terminology, rigorous reasoning, and full depth."};
const DIFFICULTIES:Record<string,string>={Easy:"basic recall and understanding; straightforward concepts and familiar examples",Normal:"understanding and application; moderate reasoning and useful distinctions",Hard:"deeper analysis, multi-step reasoning, challenging distinctions, and advanced application"};
function normalizeDifficulty(value:unknown){if(value==="Medium")return"Normal";return["Easy","Normal","Hard"].includes(String(value))?String(value):"Normal";}
function cleanJson(text:string){const fenced=text.match(/```(?:json)?\s*([\s\S]*?)\s*```/i);const candidate=(fenced?fenced[1]:text).trim();const start=candidate.indexOf("[");const end=candidate.lastIndexOf("]");if(start<0||end<start)throw new Error("The AI did not return a valid flashcard list.");return JSON.parse(candidate.slice(start,end+1));}
function normalizeQuestion(value:string){return value.toLowerCase().replace(/[^a-z0-9]+/g," ").trim();}
function validCards(items:unknown[]){return items.filter((x:any)=>x&&typeof x.q==="string"&&x.q.trim()&&typeof x.a==="string"&&x.a.trim()).map((x:any)=>({q:x.q.trim(),a:x.a.trim()}));}
async function platform(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return{};const sb=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});const{data}=await sb.from("mah_buddy_platform_settings").select("key,value").eq("is_public",true);return Object.fromEntries((data||[]).map((r:any)=>[r.key,r.value]));}
export async function POST(request:Request){try{const settings=await platform();if(settings.maintenance_mode===true||settings.flashcards_enabled===false)return NextResponse.json({error:"Flashcards are temporarily unavailable."},{status:503});const body=await request.json();const topic=String(body.topic||"General study").slice(0,180),difficulty=normalizeDifficulty(body.difficulty),learningLevel=LEVELS[body.learningLevel]?String(body.learningLevel):"Developing",age=String(body.age||"").slice(0,20),currentStudies=String(body.currentStudies||"").slice(0,160),goal=String(body.goal||"").slice(0,160),count=Math.max(1,Math.min(100,Number(body.count)||10)),previous=Array.isArray(body.previousQuestions)?body.previousQuestions.slice(-100).map(String):[];if(!process.env.GOOGLE_GENERATIVE_AI_API_KEY)return NextResponse.json({error:"Mah Buddy AI is not connected yet."},{status:503});const collected:{q:string;a:string}[]=[],seen=new Set(previous.map(normalizeQuestion));let attempts=0;while(collected.length<count&&attempts<3){attempts+=1;const remaining=count-collected.length,outputTokens=Math.min(12000,Math.max(1200,remaining*70)),prompt=`Create exactly ${remaining} DIFFERENT flashcards about: ${topic}.\nDifficulty: ${difficulty} — ${DIFFICULTIES[difficulty]}.\nLearning/language level: ${learningLevel} — ${LEVELS[learningLevel]}.\nLearner age: ${age||"not provided"}.\nCurrent studies: ${currentStudies||"not provided"}.\nLearning goal: ${goal||"not provided"}.\nUse learning level to control language, vocabulary, assumed knowledge, and explanation depth. Use age to control maturity, examples, pacing, and appropriateness. Use current studies and goal to make academic examples and terminology relevant where possible. Do not invent a specific grade, curriculum, syllabus, or subject that was not supplied.\nDo not repeat, paraphrase, or reuse any question in the excluded list.\nExcluded questions: ${JSON.stringify([...seen].slice(-100))}\nReturn ONLY valid JSON with no markdown, exactly in this shape:\n[{"q":"question","a":"answer"}]\nRules: exactly ${remaining} cards; each question must test a different concept, fact, process, example, or useful distinction; answers must be accurate and concise; do not put multiple questions in one card; never reveal a different card's answer in the question; keep wording age-appropriate and matched to the selected learning level and current studies.`;const result=await generateText({model:google(process.env.GEMINI_MODEL||"gemini-3.5-flash-lite"),prompt,maxOutputTokens:outputTokens,temperature:.35});const parsed=cleanJson(result.text),valid=validCards(Array.isArray(parsed)?parsed:[]);for(const card of valid){const key=normalizeQuestion(card.q);if(!seen.has(key)){seen.add(key);collected.push(card);if(collected.length>=count)break;}}}if(collected.length!==count)return NextResponse.json({error:`Mah Buddy could only create ${collected.length} unique flashcards out of ${count}. Please try again.`},{status:422});return NextResponse.json({cards:collected,learningLevel,difficulty});}catch(error){console.error("Mah Buddy flashcards error:",error);return NextResponse.json({error:"Mah Buddy could not create the flashcards right now."},{status:500});}}

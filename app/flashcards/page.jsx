"use client";
import {useEffect,useState} from "react";

const LEVELS=["Foundation","Developing","Proficient","Advanced","Expert"];

export default function FlashcardsPage(){
 const[topic,setTopic]=useState("");const[difficulty,setDifficulty]=useState("Normal");const[count,setCount]=useState("10");const[custom,setCustom]=useState(25);const[started,setStarted]=useState(false);const[finished,setFinished]=useState(false);const[i,setI]=useState(0);const[flip,setFlip]=useState(false);const[cards,setCards]=useState([]);const[learningLevel,setLearningLevel]=useState("Developing");const[profile,setProfile]=useState({age:"",educationLevel:"",goal:""});const[loading,setLoading]=useState(false);const[error,setError]=useState("");
 const total=Number(count==='Custom'?custom:count);
 useEffect(()=>{try{const p=JSON.parse(localStorage.getItem("mah-buddy-profile")||"{}");if(LEVELS.includes(p.learningLevel))setLearningLevel(p.learningLevel);setProfile({age:String(p.age||""),educationLevel:String(p.educationLevel||""),goal:String(p.goal||"")})}catch{}},[]);
 async function startFlashcards(){setLoading(true);setError("");setFinished(false);setI(0);setFlip(false);setCards([]);try{const response=await fetch("/api/flashcards",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({topic:topic||"General study",difficulty,learningLevel,age:profile.age,currentStudies:profile.educationLevel,goal:profile.goal,count:total,previousQuestions:[]})});const data=await response.json();if(!response.ok)throw new Error(data.error||"Could not create flashcards.");if(!Array.isArray(data.cards)||data.cards.length!==total)throw new Error("Mah Buddy did not create the exact number of unique cards requested. Please try again.");setCards(data.cards);setStarted(true)}catch(e){setError(e.message||"Could not create flashcards.")}finally{setLoading(false)}}
 if(finished)return <main className="study"><div className="result"><span>DECK COMPLETE</span><h1>{cards.length} cards</h1><p>{difficulty} difficulty • {learningLevel} learning level</p><button onClick={()=>{setStarted(false);setFinished(false);setI(0);setFlip(false)}}>New deck</button></div><style jsx>{css}</style></main>;
 if(started){const c=cards[i];return <main className="study"><header><a href="/">←</a><strong>Flashcards</strong><span>{i+1} / {cards.length}</span></header><div className="meta"><span>{topic||"General study"}</span><b>{difficulty} • {learningLevel}</b></div><div className="progress"><i style={{width:`${((i+1)/cards.length)*100}%`}}/></div><button className="card" onClick={()=>setFlip(v=>!v)}><span>{flip?"ANSWER":"QUESTION"}</span><h1>{flip?c.a:c.q}</h1><small>Tap to {flip?"see question":"reveal answer"}</small></button><div className="actions"><button disabled={i===0} onClick={()=>{setI(x=>Math.max(0,x-1));setFlip(false)}}>← Previous</button><button className="next" onClick={()=>{if(i===cards.length-1){setFinished(true)}else{setI(x=>Math.min(cards.length-1,x+1));setFlip(false)}}}>{i===cards.length-1?'Finish deck':'Next →'}</button></div><style jsx>{css}</style></main>}
 return <main className="setup"><a className="back" href="/">← Back</a><div className="hero"><span>FLASHCARDS</span><h1>Build your revision deck.</h1><p>Cards change every time and match your difficulty, learning level, age and current studies.</p></div><label>Topic<input value={topic} onChange={e=>setTopic(e.target.value)} placeholder="e.g. Biology, algebra, history"/></label><label>Difficulty<div className="choices">{['Easy','Normal','Hard'].map(x=><button key={x} className={difficulty===x?'selected':''} onClick={()=>setDifficulty(x)}>{x}</button>)}</div></label><label>Number of cards<div className="choices">{['10','20','50'].map(x=><button key={x} className={count===x?'selected':''} onClick={()=>setCount(x)}>{x}</button>)}<button className={count==='Custom'?'selected':''} onClick={()=>setCount('Custom')}>Custom</button></div>{count==='Custom'&&<input type="number" min="1" max="100" value={custom} onChange={e=>setCustom(Math.max(1,Math.min(100,Number(e.target.value)||1)))}/>}</label><div className="level"><span>Your learning profile</span><b>{learningLevel}</b><small>Age: {profile.age||'not set'} • Current studies: {profile.educationLevel||'not set'} • Goal: {profile.goal||'not set'}</small></div>{error&&<p className="error">{error}</p>}<button className="start" disabled={loading} onClick={startFlashcards}>{loading?'Creating unique cards…':'Start flashcards →'}</button><style jsx>{css}</style></main>}

const css=`*{box-sizing:border-box}.setup,.study{min-height:100svh;background:#f7f7fa;color:#18181b;font-family:Inter,system-ui,sans-serif;padding:22px max(16px,calc((100vw - 680px)/2)) 100px}.back,.study header a{color:#555;text-decoration:none;font-size:13px}.hero{margin:24px 0;padding:28px;border-radius:26px;background:linear-gradient(135deg,#eeebff,#fff 65%,#f7eafa);border:1px solid #e3e0e9}.hero span,.meta span,.result span{font-size:9px;letter-spacing:.16em;font-weight:850;color:#6d5dfc}.hero h1{font-size:46px;line-height:.98;letter-spacing:-.06em;margin:9px 0}.hero p{color:#777;margin:0;font-size:13px}.setup label{display:block;font-size:12px;font-weight:800;margin:16px 0}.setup input{display:block;width:100%;margin-top:7px;padding:13px;border:1px solid #dddde5;border-radius:13px;background:#fff;outline:0}.choices{display:flex;gap:7px;margin-top:8px;flex-wrap:wrap}.choices button{border:1px solid #dddde5;background:#fff;border-radius:11px;padding:10px 15px;font-weight:700;color:#666}.choices button.selected{background:#eeeaff;color:#6551dc;border-color:#bdb3ff}.level{margin-top:18px;padding:13px 14px;border:1px solid #e0dfe7;border-radius:14px;background:#fff;display:grid;grid-template-columns:1fr auto;gap:4px 12px}.level span{font-size:10px;letter-spacing:.08em;text-transform:uppercase;color:#777;font-weight:800}.level b{font-size:12px;color:#6551dc}.level small{grid-column:1/-1;color:#888;font-size:11px}.error{color:#b42318;font-size:12px}.start,.next{width:100%;border:0;border-radius:14px;background:#18181b;color:#fff;padding:14px;font-weight:800;margin-top:18px}.start:disabled{opacity:.45}.study header{display:flex;align-items:center;gap:14px}.study header a{font-size:22px}.study header strong{flex:1}.study header span{font-size:11px;color:#777}.meta{display:flex;justify-content:space-between;align-items:center;margin:20px 0 10px}.meta b{font-size:10px;background:#eeeaff;color:#6551dc;padding:7px 10px;border-radius:9px}.progress{height:6px;background:#e6e5eb;border-radius:99px;overflow:hidden;margin-bottom:12px}.progress i{display:block;height:100%;background:#6d5dfc}.card{width:100%;min-height:390px;border:1px solid #e0dfe6;border-radius:28px;background:#fff;box-shadow:0 18px 50px #29205012;padding:35px;display:flex;flex-direction:column;justify-content:center;text-align:center;cursor:pointer}.card span{font-size:9px;letter-spacing:.16em;color:#8e8998;font-weight:850}.card h1{font-size:30px;line-height:1.2;margin:22px 0}.card small{color:#999}.actions{display:flex;gap:10px;margin-top:16px}.actions button{border:1px solid #dddde5;background:#fff;border-radius:13px;padding:13px 18px}.actions button:disabled{opacity:.4}.actions .next{margin:0;flex:1}.result{max-width:520px;margin:20vh auto;text-align:center;padding:40px;border:1px solid #e3e0e9;border-radius:28px;background:#fff}.result h1{font-size:56px;margin:12px 0}.result p{color:#777}.result button{width:100%;margin-top:20px;padding:13px;border:0;border-radius:13px;background:#18181b;color:#fff;font-weight:800}`;

"use client";
import { FormEvent, useEffect, useState } from "react";
type Message={role:"user"|"assistant";content:string};
export default function Home(){
 const [messages,setMessages]=useState<Message[]>([{role:"assistant",content:"Hey! I'm Mah Buddy 👋 What are you studying today?"}]);
 const [input,setInput]=useState(""); const [loading,setLoading]=useState(false); const [listening,setListening]=useState(false); const [speech,setSpeech]=useState(true);
 async function sendMessage(event?:FormEvent,textOverride?:string){event?.preventDefault();const text=(textOverride??input).trim();if(!text||loading)return;setInput("");const next=[...messages,{role:"user" as const,content:text}];setMessages(next);setLoading(true);try{const r=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({messages:next})});const d=await r.json();if(!r.ok)throw new Error(d.error);setMessages(c=>[...c,{role:"assistant",content:d.text}]);if(speech&&"speechSynthesis" in window){window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(d.text);u.rate=.95;window.speechSynthesis.speak(u)}}catch(e){setMessages(c=>[...c,{role:"assistant",content:e instanceof Error?e.message:"I couldn't answer that right now."}])}finally{setLoading(false)}}
 function voice(){const SR=(window as any).SpeechRecognition||(window as any).webkitSpeechRecognition;if(!SR){alert("Voice input is not supported by this browser.");return}const r=new SR();r.lang="en-US";r.interimResults=false;r.onstart=()=>setListening(true);r.onend=()=>setListening(false);r.onresult=(e:any)=>{const t=e.results[0][0].transcript;setInput(t);sendMessage(undefined,t)};r.start()}
 useEffect(()=>()=>window.speechSynthesis?.cancel(),[]);
 return <div className="page"><aside className="sidebar"><div className="brand">Mah <span>Buddy</span></div><nav className="nav"><button>💬 AI Chat</button><button>📝 Notes</button><button>🧠 Flashcards</button><button>🎯 Quizzes</button><button>📊 Progress</button></nav></aside><main className="main"><header className="topbar"><h1>AI Study Buddy</h1><button onClick={()=>setSpeech(v=>!v)}>{speech?"🔊 Voice on":"🔇 Voice off"}</button></header><section className="content"><div className="hero"><h2>Learn smarter with Mah Buddy.</h2><p>Ask questions, understand difficult topics, revise, and practise.</p></div><div className="cards"><div className="card"><strong>📚 Explain a topic</strong><span>Simple step-by-step explanations.</span></div><div className="card"><strong>🧠 Make flashcards</strong><span>Turn notes into revision cards.</span></div><div className="card"><strong>🎯 Quiz me</strong><span>Practise what you know.</span></div></div><section className="chat"><div className="messages">{messages.map((m,i)=><div key={i} className={`message ${m.role}`}>{m.content}</div>)}{loading&&<div className="message assistant">Mah Buddy is thinking…</div>}</div><form className="composer" onSubmit={sendMessage}><textarea value={input} onChange={e=>setInput(e.target.value)} placeholder="Ask Mah Buddy anything about your studies…" onKeyDown={e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();e.currentTarget.form?.requestSubmit()}}}/><button type="button" onClick={voice} disabled={loading}>{listening?"🔴 Listening…":"🎙️"}</button><button disabled={loading||!input.trim()} type="submit">Send</button></form></section></section></main></div>;
}
